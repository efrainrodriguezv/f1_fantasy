name: 'f1_fantasy'
version: '1.0.0'
config-version: 2

profile: 'f1_fantasy'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1_fantasy:
    staging:
      +materialized: view
      +schema: staging
    oltp:
      +materialized: table
      +schema: oltp
    olap:
      +materialized: table
      +schema: olap
